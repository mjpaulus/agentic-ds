{
  "name": "ds-radio-group",
  "version": "1.0.0",
  "componentType": "molecule",
  "mutability": "generative",
  "description": "Labeled group of mutually exclusive options for a single-choice form field.",

  "usage": {
    "contexts": ["consumer-web", "enterprise-saas"]
  },

  "api": {
    "properties": [
      { "name": "name", "type": "string", "required": true },
      { "name": "label", "type": "string", "required": false, "default": "" },
      { "name": "value", "type": "string", "required": false, "default": "" }
    ],
    "slots": [
      { "name": "default", "allowedElements": [], "description": "Radio option text; each option's markup is out of scope for this first registration (see justification)." }
    ]
  },

  "tokens": {
    "consumes": [
      "--sem-color-input-border",
      "--sem-color-action-bg",
      "--sem-color-focus-ring",
      "--sem-type-label",
      "--sem-space-gap-stack",
      "--ctx-density-scale"
    ]
  },

  "constraints": [
    {
      "id": "radio-group-token-wall",
      "type": "token-usage",
      "rule": { "allowedTiers": ["sem", "ctx"], "consumesOnly": true },
      "failureBehavior": "reject",
      "rationale": "No primitive references, no raw values."
    },
    {
      "id": "radio-group-a11y",
      "type": "accessibility",
      "rule": { "standard": "wcag21aa", "rules": ["color-contrast", "focus-order-semantics", "label"], "contexts": ["consumer-web", "enterprise-saas"] },
      "failureBehavior": "reject",
      "rationale": "A radio group needs a group-level accessible name (WCAG grouping guidance), independent of each option's own label, hence the explicit `label` rule alongside contrast and focus order."
    },
    {
      "id": "radio-group-api-lock",
      "type": "api-stability",
      "rule": { "signatureHash": "<registry>" },
      "failureBehavior": "reject",
      "rationale": "First registration establishes the API hash; later drift is a rejection."
    }
  ],

  "evolution": {
    "gate": { "metric": "interaction-error-rate", "threshold": 0.05, "minSamples": 400, "windowDays": 14 }
  },

  "provenance": {
    "author": "ai",
    "model": "claude-sonnet-5",
    "createdAt": "2026-07-11T00:00:00Z",
    "justification": "Design decision: exposed a top-level `label` property intended to back the group's accessible name (via aria-label or aria-labelledby on the host), instead of requiring every caller to wrap the group in their own labelled fieldset. Rejected alternative: omitting a group-level label entirely and relying only on each individual radio option's own label, which was rejected because WCAG grouping guidance calls for one accessible name at the group level in addition to per-option labels, and leaving that entirely to callers would make an unlabeled group the default rather than the exception. Declaring the `label` accessibility rule alongside `color-contrast`/`focus-order-semantics` is the honest way to hold this component to that bar even though this first registration has no dedicated archetype to wire `label` through to an aria attribute yet -- if the generic fallback used for validation can't satisfy that rule, that is a real gap to surface, not one to quietly drop from the constraint set."
  }
}
