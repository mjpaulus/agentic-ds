{
  "name": "ds-divider",
  "version": "1.0.0",
  "componentType": "atom",
  "mutability": "generative",
  "description": "Thin visual rule separating sections of content, horizontal or vertical.",

  "usage": {
    "contexts": ["consumer-web", "enterprise-saas"]
  },

  "api": {
    "properties": [
      { "name": "orientation", "type": "string", "required": false, "default": "horizontal", "enum": ["horizontal", "vertical"] }
    ]
  },

  "tokens": {
    "consumes": [
      "--sem-color-input-border",
      "--sem-space-gap-stack"
    ]
  },

  "constraints": [
    {
      "id": "divider-token-wall",
      "type": "token-usage",
      "rule": { "allowedTiers": ["sem", "ctx"], "consumesOnly": true },
      "failureBehavior": "reject",
      "rationale": "No primitive references, no raw values."
    },
    {
      "id": "divider-a11y",
      "type": "accessibility",
      "rule": { "standard": "wcag21aa", "rules": ["focus-order-semantics"], "contexts": ["consumer-web", "enterprise-saas"] },
      "failureBehavior": "reject",
      "rationale": "A decorative divider carries no text and is never a focus stop; color-contrast is intentionally not asserted here since --sem-color-input-border has no declared ds.contrastPair (it borders content, it is not paired text/background)."
    },
    {
      "id": "divider-api-lock",
      "type": "api-stability",
      "rule": { "signatureHash": "<registry>" },
      "failureBehavior": "reject",
      "rationale": "First registration establishes the API hash; later drift is a rejection."
    }
  ],

  "evolution": {
    "gate": { "metric": "task-completion-rate", "threshold": 0.03, "minSamples": 200, "windowDays": 14 }
  },

  "provenance": {
    "author": "ai",
    "model": "claude-sonnet-5",
    "createdAt": "2026-07-11T00:00:00Z",
    "justification": "Design decision: scoped the accessibility constraint to `focus-order-semantics` only rather than also declaring `color-contrast`. Rejected alternative: declaring color-contrast anyway on the reasoning that 'every visual component should have a contrast check', rejected because --sem-color-input-border carries no ds.contrastPair extension in tokens.json (it is a border color, not a text/background pair), so a contrast constraint here would check nothing real -- declaring an unenforceable rule would be decorative in exactly the sense constraint-enforcement-spec.md Section 8 warns against. Also considered adding role='separator' and an aria-orientation binding immediately; rejected as premature for a component with no dedicated archetype yet, deferred to a later additive revision."
  }
}
