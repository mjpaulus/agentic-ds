{
  "name": "ds-avatar",
  "version": "1.0.0",
  "componentType": "atom",
  "mutability": "generative",
  "description": "Circular identity marker showing a user's initials, sized for use in lists, headers, and comment threads.",

  "usage": {
    "contexts": ["consumer-web", "enterprise-saas"]
  },

  "api": {
    "properties": [
      { "name": "initials", "type": "string", "required": false, "default": "" },
      { "name": "size", "type": "string", "required": false, "default": "medium", "enum": ["small", "medium", "large"] }
    ],
    "slots": [
      { "name": "default", "allowedElements": [], "description": "Initials fallback text; mirrors the `initials` property for cases where slotted markup is preferred over an attribute." }
    ]
  },

  "tokens": {
    "consumes": [
      "--sem-color-emphasis-bg",
      "--sem-color-emphasis-fg",
      "--sem-type-label",
      "--sem-radius-interactive",
      "--sem-space-inset-control",
      "--ctx-density-scale"
    ]
  },

  "constraints": [
    {
      "id": "avatar-token-wall",
      "type": "token-usage",
      "rule": { "allowedTiers": ["sem", "ctx"], "consumesOnly": true },
      "failureBehavior": "reject",
      "rationale": "No primitive references, no raw values."
    },
    {
      "id": "avatar-a11y",
      "type": "accessibility",
      "rule": { "standard": "wcag21aa", "rules": ["color-contrast", "focus-order-semantics"], "contexts": ["consumer-web", "enterprise-saas"] },
      "failureBehavior": "reject",
      "rationale": "Initials text against the emphasis backdrop must clear AA in both contexts; a non-interactive avatar is never a focus stop."
    },
    {
      "id": "avatar-api-lock",
      "type": "api-stability",
      "rule": { "signatureHash": "<registry>" },
      "failureBehavior": "reject",
      "rationale": "First registration establishes the API hash; later drift is a rejection."
    }
  ],

  "evolution": {
    "gate": { "metric": "task-completion-rate", "threshold": 0.05, "minSamples": 250, "windowDays": 14 }
  },

  "provenance": {
    "author": "ai",
    "model": "claude-sonnet-5",
    "createdAt": "2026-07-11T00:00:00Z",
    "justification": "Design decision: `size` selects among the existing --sem-space-inset-control / --sem-type-label token pair (both already scaled by --ctx-density-scale) rather than a dedicated per-size dimension. Rejected alternative: hardcoding a raw pixel diameter per size value (e.g. small=24px, medium=32px, large=40px) directly in generated CSS. Rejected because that is exactly the raw-value violation the token wall forbids (constraint-enforcement-spec.md Section 3) and would bypass context-driven density scaling entirely; keeping size inside the token system means avatar sizing automatically respects enterprise-saas's tighter density scale instead of needing its own per-context branch."
  }
}
